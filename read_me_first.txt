jdk 8u361
talkroom-source code是源代码
构建的时候需要User.java和服务端或者客户端一起构造
不然无法构造
剩下两个个zip是jar
jdk是openjdk8，我也放在目录里面了
先安装那个jdk，不然打不开
talkroom-source code is the source code
The build requires User.java to be built with the server or client
Otherwise it can't be constructed
The remaining two ZIps are jars
The jdk is openjdk8, which I also put in the directory
Install that jdk first, or it won't open